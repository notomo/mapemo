{
  "private": true,
  "name": "mapemo",
  "version": "0.0.1",
  "description": "map with memo",
  "scripts": {
    "build": "webpack",
    "watch": "webpack -w",
    "setup": "npm install && npm run build && gcloud init --console-only",
    "update-cors": "gsutil cors set cors.json gs://mapemo.appspot.com/",
    "upload-data": "gsutil cp data.json gs://mapemo.appspot.com/",
    "setup-firebase": "npm install -g firebase-tools && firebase login --no-localhost && firebase init",
    "serve": "firebase serve -p 5000 -o 0.0.0.0",
    "deploy": "firebase deploy",
    "deploy-storage-rule": "firebase deploy --only storage"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "firebase": "^5.10.1",
    "googlemaps": "^1.12.0",
    "vue": "^2.6.10",
    "vue-property-decorator": "^8.1.0",
    "vue-router": "^3.0.2",
    "vuescroll": "^4.12.2"
  },
  "devDependencies": {
    "@types/googlemaps": "^3.30.19",
    "css-loader": "^2.1.1",
    "prettier": "^1.16.4",
    "ts-loader": "^5.3.3",
    "typescript": "^3.4.1",
    "vue-loader": "^15.7.0",
    "vue-style-loader": "^4.1.2",
    "vue-template-compiler": "^2.6.10",
    "webpack": "^4.29.6",
    "webpack-cli": "^3.3.0"
  }
}
